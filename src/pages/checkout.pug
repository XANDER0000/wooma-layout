extends ../layouts/main

include ../components/hero-banner/hero-banner

block append data
  -
    page.name = "checkout";
    page.title = "Заказ";

block main
  h1.visually-hidden Заказ
  +hero-banner(title='Ваш заказ' breadcrumbs=[{label: "Главная", link: "home.html"},{label: "Заказ"}])
